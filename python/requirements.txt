# Python ML Dependencies for DualMind
# Install with: pip install -r requirements.txt

# Core ML Libraries — pinned to match trained model versions
numpy>=1.21.0,<2.0.0
scikit-learn==1.3.0

# Optional: XGBoost
xgboost>=1.5.0

# API Server (FastAPI + Uvicorn)
fastapi>=0.100.0
uvicorn[standard]>=0.20.0

# Data Processing
pandas>=1.3.0

# Utilities
python-dateutil>=2.8.0

# Redis (optional — for distributed caching)
redis>=4.5.0
