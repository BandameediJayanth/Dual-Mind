services:
  - type: web
    name: dualmind-ml-backend
    env: python
    rootDir: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python server.py"
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.0
